node_modules/
.env

# Hardhat build artifacts
artifacts/
cache/
typechain-types/

# Deployment output (contains addresses, not secrets — but gitignore for flexibility)
deployments.json

# Circom build outputs (regenerated by npm run compile:circuit)
circuits/ptau/*.ptau
circuits/prediction_js/
circuits/*.r1cs
circuits/*.sym
circuits/*.zkey
circuits/verification_key.json

# General build
build/
.npm-cache

# macOS
.DS_Store

models/

.claude
